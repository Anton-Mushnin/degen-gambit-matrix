import styles from "./ZzzHistory.module.css";
const ZzzHistory = ({gameId}: {gameId: string}) => {

    return (
        <div className={styles.container}>
        </div>
    )
}

export default ZzzHistory;