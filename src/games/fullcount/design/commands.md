# Fullcount player commands

- `startSession(role, requireSignature)`
- `joinSession(sessionID, signature)`
- `startAtBat(role, requireSignature)`
- `commitPitch(sessionID, signature)`
- `commitSwing(sessionID, signature)`
- `revealPitch(sessionID, nonce, speed, vertical, horizontal)`
- `revealSwing(sessionID, nonce, kind, vertical, horizontal)`
- `abortSession(sessionID)`
- `leave(sessionID)`
